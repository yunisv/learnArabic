import {useParams} from "react-router-dom";
import {Flex} from "antd";

const RTA = () => {
    let { id } = useParams();
    console.log(id)
    return (
        <Flex>
            sd
        </Flex>
    )
}

export default RTA